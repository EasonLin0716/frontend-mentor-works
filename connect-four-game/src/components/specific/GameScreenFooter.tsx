import React from 'react';
import styles from './GameScreenFooter.module.css';

const GameScreenFooter: React.FC = () => {
    return <footer className={styles.wrapper}></footer>;
};

export default GameScreenFooter;
